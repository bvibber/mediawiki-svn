// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("wmerrors", "for wmerrors support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("wmerrors", "enable wmerrors support", "no");

if (PHP_WMERRORS != "no") {
	EXTENSION("wmerrors", "wmerrors.c");
}

