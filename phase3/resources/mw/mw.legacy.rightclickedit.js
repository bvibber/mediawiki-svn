/*
 * Legacy emulation for the now depricated skins/common/rightclick.js
 */

( function( $, mw ) {

/* Extension */

$.extend( mw.legacy, {
	//
} );

/* Initialization */

$( document ).ready( function() {
	//
} );

} )( jQuery, MediaWiki );