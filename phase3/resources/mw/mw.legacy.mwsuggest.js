/*
 * Legacy emulation for the now depricated skins/common/mwsuggest.js
 */

( function( $, mw ) {

/* Extension */

$.extend( mw.legacy, {
	//
} );

/* Initialization */

$( document ).ready( function() {
	//
} );

} )( jQuery, MediaWiki );