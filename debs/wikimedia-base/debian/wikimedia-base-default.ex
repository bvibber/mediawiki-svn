# Defaults for wikimedia-base initscript
# sourced by /etc/init.d/wikimedia-base
# installed at /etc/default/wikimedia-base by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
DAEMON_OPTS=""
