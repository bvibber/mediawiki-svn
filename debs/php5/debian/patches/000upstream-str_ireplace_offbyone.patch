diff -Nurp orig/ext/standard/string.c new/ext/standard/string.c
--- orig/ext/standard/string.c	2007-02-20 15:03:55.000000000 +0100
+++ new/ext/standard/string.c	2007-02-20 15:05:18.000000000 +0100
@@ -3148,7 +3148,7 @@ PHPAPI int php_char_to_str_ex(char *str,
 	}
 	
 	Z_STRLEN_P(result) = len + (char_count * (to_len - 1));
-	Z_STRVAL_P(result) = target = safe_emalloc(char_count, to_len, len);
+	Z_STRVAL_P(result) = target = safe_emalloc(char_count, to_len, len + 1);
 	Z_TYPE_P(result) = IS_STRING;
 
 	if (case_sensitivity) {
