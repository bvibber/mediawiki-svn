/*
 * Legacy emulation for the now depricated skins/common/upload.js
 */

( function( $, mw ) {

/* Extension */

$.extend( mw.legacy, {
	//
} );

/* Initialization */

$( document ).ready( function() {
	//
} );

} )( jQuery, MediaWiki );