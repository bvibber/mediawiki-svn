import de.brightbyte.db.*;

runSql( script, table, outfile ) {
			RunIntegratorSql app = new RunIntegratorSql();
			app.setKeepAlive(true);
			app.setTweaks(tweaks);
			
			slaveInit();
			slaveLaunch(datasource, dataset, tweaks, script, table, null, outfile, null); //TODO: more params
}