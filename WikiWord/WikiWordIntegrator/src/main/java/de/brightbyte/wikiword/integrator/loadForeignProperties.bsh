import de.brightbyte.wikiword.integrator.*;

loadForeignProperties( table, source ) {
			AbstractIntegratorApp app = newIntegratorApp(LoadForeignProperties.class, table, source);
			app.slaveLaunch();
}