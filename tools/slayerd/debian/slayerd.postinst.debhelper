# Automatically added by dh_installinit
if [ -x "/etc/init.d/slayerd" ]; then
	update-rc.d slayerd defaults >/dev/null
	if [ -x "`which invoke-rc.d 2>/dev/null`" ]; then
		invoke-rc.d slayerd start || exit $?
	else
		/etc/init.d/slayerd start || exit $?
	fi
fi
# End automatically added section
