
mw.SmilBody = function( $body ){
	return this.init( $body );
}

mw.SmilBody.prototype = {
	
	init: function( $body ){
		this.$dom = $body; 
	},
	
	
	updateLayout: function ( $layoutDom ) {
		// parse out the time
		return  $layoutDom;
	}
}