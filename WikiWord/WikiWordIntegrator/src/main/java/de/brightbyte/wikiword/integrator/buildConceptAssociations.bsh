import de.brightbyte.wikiword.integrator.*;

loadForeignProperties( table, source ) {
			AbstractIntegratorApp app = newIntegratorApp(BuildConceptAssociations.class, table, source);
			app.slaveLaunch();
}